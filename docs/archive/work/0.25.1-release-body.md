# v0.25.1 — release body (archived)

Shipped wording lives in `.goreleaser.yml`; this file archives the drafted
body for the record.

## What To Watch

- **Cloud Remote is back on Windows and Linux.** v0.25.0 briefly shipped it
  macOS-only; this release restores the full platform list with fresh
  per-platform release provenance. Updating restores lost Cloud routing.

## Internal

- Reverts the #593 darwin-only publication scoping: its premise (validation
  infrastructure out of reach) was stale — both lab hosts are SSH-reachable
  via VPN, and `cloud-evidence-provenance.sh` runs the non-waivable
  per-platform provenance remotely (`HA_NOVA_LINUX_SSH`/`HA_NOVA_WINDOWS_SSH`).
- Maintainer decision 2026-08-28: shrinking the platform list is a
  user-facing feature removal and always a blocking user decision
  (AGENTS.md 0.28).
